{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    // Other rules...
    "@typescript-eslint/no-explicit-any": "off", // Disables the rule entirely
    "@typescript-eslint/no-empty-interface": "off", // Disables the no-empty-interface rule
    "@typescript-eslint/no-unused-vars": "off", // Disables the no-unused-vars rule
    "prefer-const": "off" // Disables the prefer-const rule
  }
}
